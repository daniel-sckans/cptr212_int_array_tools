#include "iat.h"

// This has to match the definition in "iat.h".  
int arr_mul(int* arr, unsigned int arr_len, int multiplier) {
    
    // Multiply each element in the array here.  

    return 0; 
}